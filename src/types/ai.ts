export type SendQueryDto = {
  message: string;
};

export type SendQuery = {
  response: string;
};

export type Queries = {
  id: number;
  user_id: string;
  query: string;
  response: string;
  date: string;
};
