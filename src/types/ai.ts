export type SendQueryDto = {
  message: string;
};

export type SendQuery = {
  response: string;
};

export type Queries = {
  id: number;
  userId: string;
  query: string;
  response: string;
  date: string;
};
